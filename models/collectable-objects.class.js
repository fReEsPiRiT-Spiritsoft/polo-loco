/**
 * CollectableObject: Base class for all collectable objects in the game (e.g. coins, bottles).
 * Inherits from DrawableObject.
 */
class CollectableObject extends DrawableObject {

    /**
     * Constructor for CollectableObject.
     * Initializes the collectable object.
     */
    constructor() {
        super();
    }
}